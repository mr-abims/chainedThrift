import { OPEN, CLOSE } from ".";

export const OPEN_ACCOUNT_DETAILS_MODAL = () => {
    return {
        type: OPEN,
    };
};

export const CLOSE_ACCOUNT_DETAILS_MODAL = () => {
    return {
        type: CLOSE,
    };
};
